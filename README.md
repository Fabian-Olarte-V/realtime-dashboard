# realtime-dashboard
Angular training project: Ticket queue dashboard with JWT auth, role-based permissions, NgRx state management, RxJS delta polling, optimistic concurrency, and performance patterns.
